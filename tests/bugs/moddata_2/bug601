puts "TODO OCC601 ALL: Error : No possibility to make a fillet 13 at the inner edge of the shape"

puts "========================"
puts " OCC601 "
puts "SAM1542"
puts "========================"
puts ""
#############################################
## No possibility to make a fillet 13 at the inner edge of the shape
#############################################

restore [locate_data_file OCC601.brep] a

explode a e

if [catch {blend result a 13 a_41 } res] {
    puts "Faulty OCC601: function BLEND works wrongly"
} else {
    puts "OCC601 OK: function BLEND works good"
    checkshape result
    
    checkprops result -s 32486.0
    set info [bopcheck result]
    if { [regexp "This shape seems to be OK" ${info}] != 1 } {
       puts "Error : No possibility to make a fillet 13 at the inner edge of the shape"
    }
    checkview -display result -2d -path ${imagedir}/${test_image}-2d.png
    checkview -display result -3d -path ${imagedir}/${test_image}-3d.png
}
