clear
smallview
if { [info exists res_2] && [isdraw res_2] } {
  donly res_2
  isos res_2 0
  fit
  isos res_2 2
} else {
  fit
}

xwd $imagedir/${test_image}.png

set info [whatis D_Second]
if { ${info} == "D_Second is a TDocStd_Document"} {
  if { [catch { Close D_Second } catch_result] } {
    puts "Error : cannot close a document D_Second"
  }
}

set ProductMode ON

puts "TEST COMPLETED"
