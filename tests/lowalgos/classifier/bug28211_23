puts "========"
puts "OCC28211"
puts "========"
puts ""
#################################################
# Modeling Algorithms - Boolean fuse operation produces incorrect result
#################################################

# The face contains gaps. However, these gaps are between curves only.
# All vertices are in the ends of correspond curves.
# Therefore, algorithm based on BRep_Tool::UVPoints does not create
# pseudo-edges.

binrestore [locate_data_file bug28211_notpseudogaps.bin] result

checkshape result

if { ![regexp {not hole} [ ishole result  ] ] } {
    puts "Error : Wrong result of 2d classifier algorithm"
} else {
    puts "OK : Good result of 2d classifier algorithm"
}

v2d
2dclear
display pp
pcurve result
2dfit

checkview -screenshot -2d -path ${imagedir}/${test_image}_2d.png
